var express = require('express');var router = express.Router();var { uuid } = require('uuidv4');const mongoose = require('mongoose');let str = 'mongodb+srv://m001-student:m001-mongodb-basics@sandbox-mkryl.mongodb.net/<dbname>?retryWrites=true&w=majority';mongoose.connect(str,{ useNewUrlParser: true }).catch(e => {  console.error('Connection error', e.message)});let users = [  {    id: '12',    username: 'smah',    firstName: 'stephanie',    lastName: 'mah'  }];/* GET users listing. */router.get('/', function(req, res, next) {  res.send(users);});router.get('/:userId', function(req, res, next) {  const foundUser = users.find(user => user.id === req.params.userId);  res.setHeader('Content-Type', 'application/json');  res.send(foundUser);});router.post('/', function(req, res, next) {  const newUser = req.body;  newUser.id = uuid();  users.push(newUser);  res.setHeader('Content-Type', 'application/json');  res.send(newUser);});module.exports = router;